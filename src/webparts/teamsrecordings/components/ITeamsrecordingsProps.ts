export interface ITeamsrecordingsProps {
  msGraphClientFactory: any,
  description: string;
  isDarkTheme: boolean;
  environmentMessage: string;
  hasTeamsContext: boolean;
  userDisplayName: string;
}
